spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dbvue
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /be-crud-vue
  port: 8082